# Source provenance

This is the auditable reuse ledger for the port. Update it whenever code, constants,
tests, documentation, or generated tables are brought in from another project.
`Reviewed` means behavior or architecture was studied; `rewritten` means new C99 code
implements that behavior; `translated` means a data table or algorithm closely follows
the named source; `copied` means content is substantially verbatim; `new` means the
design and implementation were authored for this repository against the named public
interface or local module contracts.

## Reference snapshots

All references were reviewed locally on 2026-08-27 at these immutable commits:

| Project | Revision | License | Use |
|---|---|---|---|
| [jno6809/jc_reborn](https://github.com/jno6809/jc_reborn) | `524a5803e4fa65f840379c781f40ce39a927032e` | GPLv3 | Historical C gameplay and DGDS blueprint |
| [antigerme/wilson-reborn](https://github.com/antigerme/wilson-reborn) | `2d302f56580cdcfd7a94c095e77f6bfd19f129bd` | GPL-3.0-or-later | Primary current behavior/test oracle and corrected extracted tables |
| [huntergdavis/jc_reborn](https://github.com/huntergdavis/jc_reborn) | `81c1dcc3c7c122e3d803f0332598994e604ec482` | GPLv3 | Console seams and recent PS1 work |
| [huntergdavis/johnny-castaway-ps1](https://github.com/huntergdavis/johnny-castaway-ps1) | `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | GPLv3 | Low-memory design, lazy resources, generated tests, port history |
| [huntergdavis/johnny_dreamcastaway](https://github.com/huntergdavis/johnny_dreamcastaway) | `7f2f1adf344fe309c43493e3701ae5967e4eee4f` | GPLv3 | Graphics/sound/event platform seams |
| [deckarep/Johnny-Castaway-2026-Public](https://github.com/deckarep/Johnny-Castaway-2026-Public) | `ba5ae61884ff3fb765e766d3d28507a03dea9b20` | No license file found in reviewed snapshot; all rights reserved by default | Evaluated only; no code or data reused |
| [libretro/libretro-common](https://github.com/libretro/libretro-common) | `09b55b683764d49ae1e640d7db5069931d6d2d40` | Per-file; `libretro.h` uses the MIT license text | Pinned Git submodule and API header |
| [libretro/libretro-fceumm](https://github.com/libretro/libretro-fceumm) | `236ccdfc911e84c60fea6b9d0699c2d440a8de14` | GPLv2 | Reviewed portable-core target names and devkitPro compiler/static-library conventions; no Makefile text copied |
| [libretro/gambatte-libretro](https://github.com/libretro/gambatte-libretro) | `d9d6cd06382d1ced30de34d56d3609452323dab1` | GPLv2 | Corroborated current libnx and devkitPPC target flags; no Makefile text copied |
| [libretro/RetroArch](https://github.com/libretro/RetroArch) | `96a1b1a9cf3f9166affcfd7df4323aa58d5c281a` (Web pin); `34c069f44f419b708c5362f96c54f959bc182dbe` (platform review) | GPLv3 | Pinned optional Web frontend; Emscripten build/loader behavior translated into the local launcher; current platform makefiles and advertised targets reviewed without copying code |
| [libretro/libretro-super](https://github.com/libretro/libretro-super) | `bc2b5463fe7e45a36295db54f74bc56b950dec4b` | GPLv3 | Reviewed current core target names, extensions, toolchain mappings, and platform build scripts; no recipe copied verbatim |
| [libretro/docs](https://github.com/libretro/docs) | `7a0833c074b31d5438dce6ef6a94eea7f5805ec6` | CC BY 4.0 | Reviewed official PSP, Wii, GameCube, Wii U, 3DS, PS2, Vita, Switch, Android, Linux/BSD, Haiku, and DOS compilation guides |
| [devkitPro/docker](https://github.com/devkitPro/docker), [pspdev/pspdev](https://github.com/pspdev/pspdev), [VitaSDK/docker](https://github.com/VitaSDK/docker), [ps2dev/ps2dev](https://github.com/ps2dev/ps2dev) | `bee28ad017637fb386814e91220470309e88bbf0`; `8d581609506117f6c8fa77893a2f38e4ad4b39fe`; `ec318d0ace94757b81f4fba16a1be86baea647c0`; `0463697a324b3d3c000636e6fd12324b0f2a8df2` | Respective upstream licenses | Official SDK-container sources reviewed; only immutable image references and invocation logic are recorded in this repository |
| libnx; libctru; wut | `7644c9b26099aa2d2145bc72a21ee24190e92085`; `36fe1ada5b7ebe53ba4decda36d764a55f8fefb6`; `2016e429c16dab366d902455b43f21ccb520e23d` | ISC; Zlib; Zlib | Exact runtime notices packaged under `docs/licenses/frontend/`. Upstream source-file hashes: libnx `LICENSE.md` `5b6675f85016009624d29c9222201d3c0fc6a2d5c59253367f27d3e15011cdb2`; libctru `README.md` `6ca781f919d70afba6206e61415ac8bd003adce37fc08c10d8367c9114c77fd0`; wut `LICENSE.txt` `61b4a8df7082243224f15eb3d7f799aa0c71d774de6d7db879a43cef4e579d64` |
| [hrydgard/ppsspp](https://github.com/hrydgard/ppsspp) | `v1.20.4`; official `PPSSPP-v1.20.4-anylinux-x86_64.AppImage` SHA-256 `661c098e6b7f7610171a57b7c533ce8bba6f2312b71e76d61e850461973eba21` | GPL-2.0-or-later | Pinned external PSP emulator smoke input; downloaded only to ignored cache and never redistributed |
| [libretro/retroarch-assets](https://github.com/libretro/retroarch-assets) | `73106363e14e34c08a5854b4cfbc29f184e3b783` | CC BY 4.0 | Pinned optional Web menu/assets input; copied only into generated `build/` distributions |
| [jvilk/BrowserFS](https://github.com/jvilk/BrowserFS) | `76fd5122fcf3ad6bff3315550aafb041cfb6a72e` | MIT plus adapter notices | Official RetroArch Web filesystem dependency; license notice included, minified runtime copied only into generated `build/` distributions |

The PS1 snapshot was verified from the local checkout
`/home/hunter/workspace/jc_reborn`: its `origin` is
`huntergdavis/Johnny-Castaway-PS1` and its `HEAD` is the revision shown above. The
checkout directory name is not a separate source project. Provenance records use the
public repository identity rather than that local directory name.

The local cross-session `deja` recall search was run before implementation but returned
no reusable session record within the query timeout. Reuse claims below therefore cite
repository files and revisions directly.

## File-level derivation

| This repository | Upstream source | Derivation |
|---|---|---|
| `src/jc_story_data.inc` | `jc_reborn/story_data.h` | Copied 63-entry story table and original GPL/copyright header; filename changed and included privately |
| `src/jc_director.c`, `include/jc_director.h`, `tests/test_director.c` | Johnny Reborn `story.c`, `story_data.h`; Wilson `crates/wilson-engine/src/story.rs`; PS1 `src/host/story.c` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | Rewritten C99 director. Scene selection, 11-day wrap, holiday ranges, tide/raft/position rules, original 8-hour and optional 24-hour night modes, and the re-drawn ambient bound follow Johnny Reborn/Wilson. The one-time 6–19 intermediate budget, bounded retry/no-immediate-repeat rules, walk insertion, opening/final eligibility, and first-sequence rollover policy were verified against the pinned PS1 story loop and locked by a deterministic golden trace; no function body was copied |
| `src/jc_story_player.c`, `include/jc_story_player.h`, `tests/test_story_player.c` | The director above; PS1 `src/host/story.c` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | New nonblocking C99 controller for libretro. It consumes one director-planned scene at a time, advances plans/days at scene boundaries, and derives reproducible runtime seeds from a plan seed/day/index tuple. The blocking PS1 platform loop was a behavioral oracle; its control-flow code was not copied |
| `src/jc_rng.c`, `include/jc_rng.h` | Wilson `crates/wilson-engine/src/rng.rs` | Close C99 translation of splitmix64 seed scrambling plus xorshift64 generator for cross-platform deterministic tests |
| `src/jc_path_data.inc` | Wilson `crates/wilson-engine/src/calcpath_data.rs` | Mechanically translated Rust initializer. The upstream table was generated by `docs/reverse-engineering/extract_calcpath.py` from the user's original executable; no executable bytes are distributed here |
| `src/jc_path.c`, `include/jc_path.h`, `tests/test_path.c` | Wilson `crates/wilson-engine/src/path.rs` | Rewritten C99 weighted step planner and deterministic simple-route fallback; exhaustive tests port the upstream invariants |
| `src/jc_walk_data.inc` | `jc_reborn/walk_data.h` | Copied embedded walk frame, bookmark, and heading tables; attribution banner added because the upstream data header relies on its repository-level GPLv3 license |
| `src/jc_walk.c`, `include/jc_walk.h`, `tests/test_walk.c` | `jc_reborn/walk.c`; Wilson `crates/wilson-engine/src/walk.rs` | Nonblocking C99 walker translated from Wilson's state-machine port over Johnny Reborn's extracted data; exhaustive termination and behind-tree tests follow Wilson |
| `src/jc_island_walk.c`, `include/jc_island_walk.h`, `tests/test_island_walk.c` | PS1 `src/scene/island.c`, `src/ads/island_walk.c.inc`, and `src/walk/` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe`; this repository's `jc_walk`, BMP, and surface contracts | New bounded persistent-island compositor behaviorally derived from the pinned GPL PS1 port. It captures/restores a clean indexed island canvas, advances the existing nonblocking walker, composes `JOHNWALK` frames at island offsets, and redraws the `BACKGRND` tree cover frames for behind-tree occlusion. Allocation/ownership, arbitrary-canvas validation, checked coordinate arithmetic, error reporting, and synthetic test sprites are new; no PS1 GPU code or original BMP pixels were copied |
| `src/jc_fade.c`, `include/jc_fade.h`, `tests/test_fade.c` | PS1 `src/host/graphics.c` function `grFadeOut` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | Rewritten dimension-independent XRGB8888 transition masks. The five transition shapes/order and 50 Hz step counts follow the GPL oracle. Generalized dimensions and color, integer-only geometry, state validation, and complementary fade-in behavior are new; no SDL drawing code was copied |
| `src/jc_bmp.c`, `include/jc_bmp.h`, `tests/test_bmp_compositor.c` | Wilson `crates/wilson-dgds/src/bmp.rs`, `chunk.rs`, `pixels.rs`; Johnny Reborn `resource.c`, `graphics.c`; Dreamcast `resource.c`, `graphics.c`; Hunter fork `graphics.h`, `graphics_ps1.h` | Independently written bounded C99 parser and synthetic tests. The sources establish chunk order, packed-block fields, sequential 4bpp frames, high-nibble-first pixels, and historical 120/255 frame bounds; no source was copied verbatim |
| `src/jc_compositor.c`, `include/jc_compositor.h`, `tests/test_bmp_compositor.c` | Wilson `crates/wilson-engine/src/surface.rs`; Johnny Reborn and Dreamcast `graphics.c` | Independently written compositor/API/tests using verified transparent-key, horizontal-flip, clipping, and ordered-layer behavior; all ownership, validation, checked arithmetic, and surface-view glue are new C99 code |
| `src/jc_wav.c`, `include/jc_wav.h`, `tests/test_audio.c` | Wilson `crates/wilson-dgds/src/exe_sound.rs`, `docs/knowledge-base/04-engine-scripting-opcodes.md`, `05-arquitetura-do-engine.md`; Johnny Reborn and Dreamcast `sound.c` | Independently written bounded RIFF parser. Sources establish unsigned 8-bit mono PCM at 11025 Hz, word-aligned chunks, IDs 0–24 with 11/13 absent, filenames, and payload-size inventory; no parser code or samples were copied |
| `src/jc_audio.c`, `include/jc_audio.h`, `tests/test_audio.c` | Hunter fork `sound_ps1.c`, `sound_ps1.h` introduced at `1f97b08ca0f48e8a2f2b785acefc3a7fedab3342`; Hunter PS1 `docs/ps1/api-mapping.md`, `ps1-branch-cleanup-plan.yaml`; Johnny Reborn/Dreamcast sound code; PS1 ocean commits `bb32de68a`, `c194f1bf5` | Independently written deterministic mixer. The eight-channel and 25 original-ID bounds follow Hunter's GPLv3 PS1 design; resampling, stereo mixing, clipping, round-robin control, validation, pointer-free serialization, reserved ambience ID, per-sample gain, and seamless loop-at-EOF behavior are new C99 code. No SPU/VAG/device code or audio binary was copied |
| `src/jc_sfx.c`, `include/jc_sfx.h`, `tests/test_sfx.c`, `tools/check_sound_bank.c`, `docs/OPTIONAL_ORIGINAL_AUDIO.md` | PS1 `src/host/sound.c` and `src/platform/ps1/sound_ps1.c` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | New bounded VFS/stdio sibling-file loader over this repository's WAV/mixer ownership APIs. Only the `sound<ID>.wav` naming convention, 0–24 legal script-ID range, and documented absence/silent-no-op behavior of assets 11/13 are behaviorally derived. Path handling, 1 MiB per-file limit, failure reporting, rollback, lifetime management, exhaustive synthetic ID checks, and the authentic-data load/signal/voice-cleanup validator are new. No WAV bytes are copied or distributed; every sample remains optional user-supplied Sierra/Dynamix data |
| `src/jc_ads.c`, `include/jc_ads.h`, `include/jc_script_types.h` | Wilson `crates/wilson-dgds/src/ads.rs`, `docs/knowledge-base/04-engine-scripting-opcodes.md`; Johnny Reborn `ads.c`, `resource.c` | Independently written bounded C99 ADS parser/decoder with caller-owned decompression, structured errors, and hard resource/tag/opcode limits; no source text copied |
| `src/jc_ttm.c`, `include/jc_ttm.h`, `include/jc_script_types.h` | Wilson `crates/wilson-dgds/src/ttm.rs`, opcode knowledge base; Johnny Reborn `ttm.c`, `resource.c` | Independently written bounded C99 TTM parser/decoder supporting the 39-opcode reference table; no source text copied |
| `src/jc_script_vm.c`, `include/jc_script_vm.h`, `tests/test_script_vm.c` | Wilson `crates/wilson-engine/src/ads_vm.rs`, `ttm_exec.rs`; Johnny Reborn `ads.c`, `ttm.c` | Independently written callback/event-driven 50 Hz VM. Deterministic weighted ADS selection, threads/triggers/GOSUB, TTM delays/timers/gotos, scene lifecycle, and instruction events follow referenced behavior with new hard execution limits and synthetic tests. All 51 disposable real ADS/TTM resources parsed in validation; none are committed |
| `src/jc_runtime.c`, `include/jc_runtime.h`, `tests/test_runtime.c` | Wilson `crates/wilson-engine/src/ads_vm.rs` at `2d302f56580cdcfd7a94c095e77f6bfd19f129bd`; Johnny Reborn `ads.c`, `resource.c` at `524a5803e4fa65f840379c781f40ce39a927032e`; this repository's parser/resource/renderer contracts | Independently written bounded archive-to-ADS orchestrator. Resource declaration order, exact numeric TTM slots, and duplicate-slot replacement follow the GPL references; ownership, exact-size allocations, aggregate limits, structured rollback, VFS binding, and public API are new. Synthetic end-to-end tests cover content lookup through rendered palette/sprite/audio events and deterministic cadence; no game resource is copied |
| `src/jc_ttm_renderer.c`, `include/jc_ttm_renderer.h`, `tests/test_ttm_renderer.c` | Wilson `crates/wilson-engine/src/surface.rs`, `ttm_exec.rs`; Johnny Reborn `src/host/graphics.c`, `ttm.c` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | Independently written bounded VM-event render bridge. It implements SCR/PAL/BMP resource callbacks, transparency, slot ownership and aggregate budgets, clip/sprite/flip/primitives, saved background/zones, per-thread layers, and ordered composition. The pinned host `grNewLayer` locates palette index 0 (`#A800A8`) and enables SDL color-keying, so primitive foreground/background color 0 exposes the lower layer just like keyed BMP pixels. The local bridge maps only a primitive color equal to its configured source key into its internal 255 sentinel; authentic TTM/BMP indices are 4-bit 0–15, so the sentinel is unambiguous for the real corpus. Synthetic point/line/rectangle and both circle-color regressions prove key erasure and non-key opacity. Authentic MARY3 evidence identifies `SASKDATE.TTM` tag 150 color-0 circles at ticks 1815–2173, including `(171,133,38,38)` at tick 1845/frame 239; raw color 0 previously left a 10-pixel exact-key run at x185–194/y133, while the host key hides it and the PS1 primitive path is a stub. The pinned host renderer's validated no-op behavior for an in-range unavailable sprite frame is reused for authentic scheduler handoffs and recycled image slots; invalid slot indices, failed loads, and malformed surfaces remain errors. Synthetic VM integration and all-63-chapter authentic regressions exercise the callback path; audio/control and dump-only events are deliberately forwarded |
| Partial-height `LOAD_SCREEN` replacement in `src/jc_ttm_renderer.c` and regressions in `tests/test_ttm_renderer.c`, `tests/test_libretro.c` | GPLv3 PS1 `src/graphics_ps1/screen_load.c.inc::grLoadScreen` and host `src/host/graphics.c::grLoadScreen` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | Independently rewritten C99 lifecycle behavior. Both pinned implementations invalidate the saved-zone layer when loading an SCR, while the PS1 partial-height path copies only available source rows and preserves already seeded tiles below them. The local renderer therefore clears `saved_zones`, overlays the decoded SCR at `(0,0)`, and leaves the background outside that rectangle intact. Synthetic coverage proves stale saved overlays are removed without erasing preserved outer pixels; the user-owned authentic `JOHNNY.ADS:1` regression proves the clock saved zone exists before and is gone after the `THEEND.SCR` replacement. No GPL function body or original screen pixels were copied |
| `src/jc_captions.c`, `include/jc_captions.h`, `tests/test_extras.c` | PS1 `data/ps1/captions.json`, `src/platform/ps1/ps1_captions.c`, `docs/ps1/caption-audit-2026-04-26.yaml`; commits `23f274e97`, `795539901`, `8c22c582a`, `38b3c8cc3`, `839b0ccd6` | Translated the fresh-authored 79-entry text catalog and all 63 audited ADS/tag mappings; independently wrote the portable immutable lookup and 50 Hz state APIs. No Sierra text/media was copied |
| `src/jc_caption_render.c`, `include/jc_caption_render.h`, `tests/test_caption_render.c` | PS1 `src/platform/ps1/ps1_captions.c`, introduced by commit `795539901f4e53b2fd79ba71b14b5d51e6af7983` | Reused the GPL PS1 port's presentation policy—centered lines, bottom placement, and a dark backing band—then independently implemented a bounded XRGB8888 software renderer, wrapping/layout, opacity blending, clipping, and conventional 5x7 ASCII glyph shapes. No PS1 GPU code, coordinates, font atlas, or media was copied |
| `src/jc_chapters.c`, `include/jc_chapters.h`, `tests/test_extras.c` | PS1 `scripts/build-scene-explorer-data.py`, `src/pause_menu/scene_explorer_data.h`, generated `SCEXPL.DAT`, `config/ps1/cd_layout.xml`; commits `1c0435e13`, `86b2a62d5`, `3d673b48f`, `ba9d86bfe`, `ac483232d`, `eb3b3d690` | Translated 63 validated scene records: ADS family/tag, caption ID, title, FG2-derived frame count/capture timing, and exact `SX*.SCR` 320x240 RGB555 menu-preview name. Independently wrote catalog validation/lookup APIs. No preview image or original game resource is distributed |
| `src/jc_extras.c`, `include/jc_extras.h`, `tests/test_extras.c` | PS1 `holidays.yml`, `src/scene/holidays.c`, `src/scene/holidays_table.c`; commits `d45de0f08`, `cc2ef4d69`; public Meeus/Jones/Butcher Easter method | Translated the 36 holiday rows/rules and independently wrote bounded portable date lookup/config APIs |
| `src/jc_holiday_overlay.c`, `include/jc_holiday_overlay.h`, `tests/test_holiday_overlay.c` | Hunter Davis PS1 `src/scene/holidays.h`, `src/scene/holidays.c`, `src/pause_menu/option_helpers.c.inc`; commits `d45de0f08f87e2118114b69333006db9c8d4d937`, `cc2ef4d69afd5fcc41b781df798fd13e0acf91ee`, `0a3ef05ecf5e154a35b50fe9ace3c1aaa25e49d0` at reviewed revision `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | Reused the GPLv3 PS1 interaction policy of date-driven selection, explicit none, manual selection, and title/date preview. Independently wrote the C99 choice/parser/resolver and deterministic XRGB8888 presentation over this repository's 5x7 renderer. All 38 option values are synthetic-tested; no PS1 GPU code, generated sprite data, or proprietary `HOLIDAY.BMP` media was copied |
| `src/jc_ocean_vag.inc`, `src/jc_ocean.c`, `include/jc_ocean.h`, `tests/test_ocean.c`, `jc_ambience_*` declarations | PS1 `jc_resources/extracted/snd/OCEAN.VAG` and feature commits `bb32de68aad33ecd1b8a7fbdc0a9278a35971238`, `c194f1bf5716b460ba7276dcb8116b24a690c636`; BigSoundBank sound 0266 “Sea: Waves” | Mechanically embedded the exact CC0/public-domain-derived VAG bytes (SHA-256 `b9eeae5a7f42545ad7fe99701c248c07e8b4c0ad0ab17bb86420f36ea97259c2`). New GPL wrapper/test code exposes bounded decoding and validates the 11025 Hz mono stream, `[28, 220528)` loop window, and decoded PCM hash. Source/license details ship in `docs/licenses/BigSoundBank-0266-CC0.md` |
| `src/jc_vag.c`, `include/jc_vag.h`, `tests/test_vag.c` | PSX-SPX SPU ADPCM documentation; PS1 `scripts/wav2vag.py` at revision `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | Independently written bounded C99 VAGp/SPU ADPCM probe and unsigned-8 PCM decoder. Predictor coefficients and block flags follow public hardware documentation; high-predictor/low-shift and low-nibble-first packing were verified against the PS1 encoder. No decoder code was copied |
| `src/jc_resource_map.c`, `src/jc_content.c` | Johnny Reborn `resource.c`; Wilson `crates/wilson-dgds/src/archive.rs`, `resource.rs`; Hunter PS1 resource-loading work | Independently written bounded C99/VFS implementation informed by the formats and lazy-loading architecture |
| `src/jc_decompress.c` | Johnny Reborn `uncompress.c`; Wilson `crates/wilson-dgds/src/decompress.rs`; ScummVM DGDS research cited by Wilson | Independently written bounded C99 RLE/LZW decoder using documented DGDS behavior |
| `src/jc_palette.c`, `src/jc_scr.c`, `src/jc_surface.c` | Johnny Reborn `graphics.c`; Wilson `crates/wilson-dgds/src/pal.rs`, `scr.rs`, `pixels.rs`; Wilson engine `surface.rs`; Hunter PS1 indexed-surface work | Independently written C99 decoder/raster layer informed by the named implementations and low-memory constraints |
| `src/libretro_core.c`, `include/jc_story_options.h`, `tests/test_libretro.c`, `johnny_castaway_libretro.info` | Pinned `external/libretro-common/include/libretro.h`; libretro core/options and serialization conventions; this repository's PS1-derived story/chapter/caption/holiday/ocean modules; PS1 `src/host/story.c`, `src/pause_menu/option_helpers.c.inc`, `src/scene/island.c`, and `src/ads/foreground_bridge.c.inc` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe` | New adapter and synthetic mock-frontend code written against the official API. Dynamic values expose Automatic, Static Screen, all 63 audited chapters, 38 holiday modes, deterministic seed presets, system/simulated calendar fields, 1x–4x playback, and automatic/forced tide and raft stage. The tide/raft value cycles, override precedence, `NORAFT` rule, `BACKGRND.BMP` island/shore frame numbers and coordinates, `MRAFT.BMP` stage frames and high/low-tide coordinates, and the `BACKGRND.BMP` high/low-tide foam positions, three-frame cycles, four-frame initial seeding, and eight-tick advance cadence follow the pinned GPL PS1 sources; no sprite pixels or function body was copied. The new wave phase calculator is derived solely from the already serialized runtime tick count, and temporary background restoration keeps repeated composition idempotent while preserving behind-character layer order. The seed/calendar/speed plumbing, dynamic visibility, fixed audio cadence, and backward-compatible state packing are independently designed for libretro. Automatic mode consumes the new story controller; chapter mode starts one ADS/tag; both drive downstream optional user-supplied SFX cues, non-destructive captions, the asset-free holiday overlay, and separately licensed CC0 ambience. The independently designed little-endian v2 state envelope stores stable indices and timeline scalars, embeds the existing base/audio payload for compatibility, and reconstructs owned archive/runtime/renderer state through bounded deterministic replay without serializing raw pointers. Its marker-gated compact story payload adds exact planning-calendar inputs, effective tide/raft, and the first/repeat-plan phase; marker-absent v2 states retain their original restore behavior. No original game fixture is copied |
| Authentic island-background reconstruction in `src/libretro_core.c` and exact-key regressions in `tests/test_libretro.c` | GPLv3 PS1 `src/scene/island.c` and `src/host/graphics.c` at `25c5d84593ac20cbee354eaab7779ab7397d6bbe`; local measurement of user-owned authentic `JOHNCAST.PAL` | Independently rewritten C99 composition follows `islandInit()`'s ordered day `OCEAN00`–`OCEAN02`/night `NIGHT` SCR selection, y=0 native SCR origin, raft-before-island layer order, `BACKGRND.BMP` frames 0/13/12/14 at `(288,279)`/`(442,148)`/`(365,122)`/`(396,279)`, low-tide frames 1/2 at `(249,303)`/`(150,328)`, and `MRAFT.BMP` stage positions `(512,266)` or `(529,281)`. Exact foam pairings are high-tide frames 3–5 at `(270,306)`, 6–8 at `(364,319)`, and 9–11 at `(518,303)`; low-tide frames 39–41 at `(129,340)`, 30–32 at `(233,323)`, 33–35 at `(367,356)`, and 36–38 at `(558,323)`, with three-frame cycles, four initial advances, and eight-tick cadence. The pinned host renderer locates palette color `#A800A8` for transparency; authentic `JOHNCAST.PAL` was observed locally to place it at index 0. Native regressions reject horizontal runs of eight or more exact-key pixels during fixed chapter playback, Automatic playback/walk, and state restoration. The first clean composed frame is presented before returning to the frontend, matching the reference initialization order. Day scenes currently select deterministic `OCEAN00` only; unlike the GPL reference, variants 01/02 are not randomly selected until a backdrop-variant value is serialized. No GPL function body and no original SCR/BMP/PAL byte or pixel data was copied |
| Fixed-chapter tide and raft overrides in `src/libretro_core.c` and `tests/test_libretro.c` | Same pinned GPLv3 PS1 island sources above; this repository's existing Automatic-mode option policy and `jc_story_effective_raft_stage()` contract | New adapter wiring applies the already documented tide and raft menu policy to the temporary island state used for fixed chapters. Authentic user-owned-resource regressions prove forced high/low tide and raft stages 0/5 produce distinct fixed-chapter compositions; the shared resolver retains `NORAFT` precedence. No new upstream code or media was copied |
| `tools/check_all_chapters.c`, `tools/check_all_chapter_visuals.c`, `docs/SCENE_ACCEPTANCE_MATRIX.md`, generated ignored `build/scene-*-results.csv` evidence | PS1 `docs/ps1/scene-status.md`, `docs/ps1/visual-detection-spec.md`, `src/pause_menu/scene_explorer_data.h`, `src/foreground_pilot/runtime_memory.c.inc`, `scripts/build-scene-explorer-thumbnails.py`, and tracked 63-scene regtest summaries at `25c5d84593ac20cbee354eaab7779ab7397d6bbe`; this repository's chapter/director/libretro contracts | New metadata-only acceptance tooling and report. The native VM tool records ordered `PLAY_SAMPLE` ID/tick/frame events, rejects missing/out-of-range IDs, and labels legal absent IDs 11/13 as expected silent no-ops. The mock frontend selects all 63 chapters at forced high and low tide plus six static screens, observes every production-presented frame, enforces bounded completion, meaningful output, exact-key run/component, phase/hash diversity, scene-class color signatures, island tide differences, and non-island invariance, and records start/middle/final hashes. Required `deja` queries found prior PS1/Scene Explorer context but no reusable detailed matrix or MARY3 key fix. No local PS1 reference screenshot, generated thumbnail, decoded pixel, script, content archive, or audio sample is copied; CSV evidence under `build/` is ignored |
| `Makefile`, `patches/retroarch-wii-single-core.patch`, `scripts/build-target.sh`, `scripts/build-apple-universal.sh`, `scripts/build-console-cores.sh`, `scripts/prepare-pinned-ps2-make.sh`, `scripts/build-psp-frontend.sh`, `scripts/test-psp-ppsspp.sh`, `scripts/build-installable-frontends.sh`, `tools/check_console_archive.py`, `tools/check_installable_frontend.py`, `tools/make_deterministic_zip.py`, `tools/test_deterministic_zip.py`, `docs/CONSOLE_BUILDS.md`, `docs/PSP_PACKAGE.md`, `docs/INSTALLABLE_FRONTENDS.md`, `docs/FRONTEND_SDK_NOTICES.md` | libretro platform conventions; pinned FCEUmm and Gambatte `Makefile.libretro`; pinned RetroArch platform makefiles; official libretro console compilation/install guides; prior Hunter console ports; pinned SDK-container/tool/emulator sources listed above; current devkitPro/libogc public headers and runtime contract | New project build mappings, Docker orchestration, archive/package validation, deterministic ZIP normalization, and installable frontend packaging. The unified builder links strictly validated Switch, 3DS, GameCube, Wii, Wii U, Vita, and PS2 archives into isolated worktrees at the pinned RetroArch revision, replaces generic metadata with fixed Johnny identities, carries legal/provenance records and available SDK license trees, and rejects original game data. Archive creation uses deterministic `ar rcsD` metadata and per-target manifests bind the validated archive SHA to the Johnny commit, tree state, platform, and immutable SDK image. GameCube uses the independently reproduced supported current-libogc flags. Wii applies the new GPLv3-or-later tracked patch to conditionally omit pinned RetroArch's GPLv2-only `wii/app_booter` and disable re-exec/core switching; it retains the static Johnny core, RGUI/content browser, and GX runtime, embeds a Wii-specific identity, packages the patch itself, and binds the patch SHA plus exact no-reexec/current-libogc flags in provenance. A required `deja` search found no prior reusable Wii implementation. Wii U packages pinned wut `elf2rpl`'s explicit uncompressed large-RPX target, avoiding an extra compressor. PS2 uses Alpine `make-4.4.1-r4.apk` SHA-256 `a916626a876f64c6b6fb32a6bf0fa93351d09fae0aa7684135642390da647e60`; its extracted binary SHA-256 is `a75fd4f07fadd6506b7be7d4684af96d22d6cebf7847acdad4270d503d020f63`, cached ignored and mounted read-only so offline builds never access the network. The PSP emulator gate verifies the exact official PPSSPP AppImage hash and a 30-second EBOOT/module/GE/framebuffer run. No third-party SDK/frontend/tool/emulator or proprietary data is copied into the repository or packages |
| Connected-component color-key gate in `tools/web_smoke_test.py` and its documentation in `docs/WEB_TESTING.md` | This repository's indexed-renderer `#A800A8` transparent-key contract and observed Firefox RGB565 quantization to approximately `#AD00AD` | Independently written, standard-library-only frame classifier. It retains the broad lost-frame magenta guard, labels a narrow renderer-key color window, computes the largest four-connected component and its bounds, and fails any component of at least 256 pixels spanning at least 16×4. This catches residual key blocks even when scattered key-like palette colors keep the total fraction below the old dominant-frame threshold. It also records the largest solid rectangle as diagnostic evidence. No user-owned pixel buffers or screenshots are committed; only ignored local evidence records metrics |
| `scripts/build-web-player.sh`, `scripts/serve-web.sh`, `scripts/stage-local-web-content.sh`, `tools/check_web_dist.py`, `tools/test_web_dist_validator.py`, `tools/serve_web.py`, `tools/web_smoke_test.py`, `web/`, `docs/WEB_TESTING.md` | Pinned RetroArch `pkg/emscripten/README.md`, `pkg/emscripten/libretro/libretro.js`, `Makefile.emscripten`, `audio/drivers/rwebaudio.c`, and `emscripten/library_rwebaudio.js`; Hunter Davis `jc_reborn/docs/play/online/player.js` at GPLv3 commit `6316545c0c` for its audio-state/user-gesture unlock design; pinned retroarch-assets and BrowserFS notices listed above; W3C WebDriver Recommendation and Mozilla geckodriver usage documentation linked in `docs/WEB_TESTING.md`; `make_synthetic_content()` in `tests/test_libretro.c` | New local build/validation/server/page and standard-library browser-test code. Module initialization, BrowserFS mount layout, fake static-core path, and command bridge are derived from the GPL RetroArch loader and credited in `WEB_PLAYER_NOTICE.md`. The visible audio state, explicit Enable Audio control, `resume()` policy, and pointer/keyboard/touch capture adapt the pinned Hunter player. Because this linked RetroArch module does not promise a public `RWA` handle, this port independently wraps `AudioContext.prototype.createBufferSource` before module startup to retain the actual contexts without inspecting sample data; state listeners and multiple-context handling are new. A native-constructor subclass retains the returned real context when the pinned RWebAudio driver short-circuits after a blocked initial `resume()` and therefore never reaches `createBufferSource`; it preserves native `instanceof`, prototype behavior, static inheritance, and scheduling. The query-gated test probe observes only Web Audio scheduling metadata; its 10 ms buffer and gap assertions follow the pinned GPL RetroArch driver/library, while it records no sample bytes. Optional local auto-start fetches the fixed resource pair and opportunistically fetches the 23 possible sibling WAV names from `local-content/`; it writes them only to the browser's in-memory content directory. The staging helper copies user-owned files only beneath ignored `build/`, and the source checker rejects tracking those names. The Web build provenance binds the full Johnny commit, clean/developer tree state, version, core metadata, BrowserFS, and WebAssembly hashes; its checker and four adversarial fixtures require internal consistency, while the release assembler byte-compares every project Web file and bundled `.info` to the exact release commit. The browser runner independently implements only the W3C HTTP/actions subset it needs, uses RetroArch's documented keyboard mapping/order as exercised from the pinned frontend, and validates settled authentic multi-frame playfield/water motion, preservation of meaningful nonblank/non-color-key frames, independent idle-window audio cadence/underrun evidence, stable gameplay, Quick Menu, Core Options, Story, and dynamically expanded simulated-calendar frames; no Selenium, geckodriver, or other browser-library source was copied. Its optional blocked-autoplay mode starts staged authentic content without input, proves changing WebGL-backed canvas frames while Firefox reports a suspended context, activates only the visible audio button, requires the context to remain running across a RetroArch reset, then applies the same strict cadence gate. Its optional `--content-dir` uploads only an explicit user-owned pair to Firefox's in-memory filesystem and may install one source-validated fixed chapter option for deterministic scripted-motion acceptance. Its explicit staged-local mode instead allowlists the pair plus 23 supported WAV names, validates a temporary pristine copy with the private directory excluded, makes no upload/start call, and requires the page's automatic fetch path to load every WAV. Its embedded 61-byte MAP and 1,126-byte archive are exact generated test-fixture outputs, guarded by SHA-256 `604d0158a6f0ec5a7ede1aee6ccd163f93889778705e8f5ae25f5880ad4301fe` and `6db50124d8a0fcdd48812c526688115886d935727780a59bad86a82cfbca807c`. The synthetic browser harness overrides Automatic with fixture-backed `fishing1` because the five-resource archive intentionally lacks the full story catalog; authentic auto-load and fixed-chapter motion are exercised separately with user-owned data. No original game data or third-party generated binary is committed |
| Query-gated WebGL diagnostics in `web/jc-web-player.js`, `tools/web_smoke_test.py`, and `tools/test_web_dist_validator.py` | Standard WebGL 1/2 JavaScript API contracts; pinned RetroArch WebGL frontend above was observed only to identify the relevant upload/draw boundary | Independently written smoke-only metadata probe. It counts texture upload, draw, clear, and context loss/restoration calls. For typed video-sized uploads it samples at most 64 evenly spaced bytes into an ephemeral-secret-salted rolling 32-bit signature and distinct-signature count. Neither the salt, sampled bytes, complete pixels, frames, user resources, nor audio are exposed or retained. Production pages without `?smoke=1` do not install the probe. The existing CI-run Web validator test enforces the query gate, bounded sampling, hidden salt, absence of pixel/frame buffers, and result-schema fields |
| `.github/workflows/ci.yml`, `.github/workflows/console-cross.yml`, `scripts/test-native-retroarch.sh`, `tools/check_core_exports.py`, `tools/check_web_sources.py` | Official GitHub `actions/checkout` and `actions/upload-artifact` releases; official runner inventories; official Emscripten SDK 3.1.69; Ubuntu cross-toolchain/RetroArch packages; pinned public console containers, RetroArch, and PPSSPP input listed above | New CI and validation code. Normal CI builds strict host, Linux, Windows, Android, Emscripten, macOS, iOS, and tvOS targets and verifies exact libretro ABIs/machines. Its native smoke executes the core in Ubuntu RetroArch and verifies options, VFS, geometry, content indexing, clean unload, persistence, and material frames. Manual console CI builds eight deterministic core archives, the PSP EBOOT/install ZIP plus pinned 30-second PPSSPP smoke, and all seven unified installable frontends with legal/no-data/package gates; it publishes those outputs in the two console source artifacts. CI also rejects tracked original data and remote Web dependencies and gates the expensive full Web distribution behind explicit input |
| `scripts/assemble-release.sh`, `docs/RELEASING.md` | Ten exact Actions source artifacts; this repository's format/archive/Web validators; seven validated unified frontend ZIPs nested in the console source artifact; GitHub CLI run/artifact/release interfaces | New fail-closed release assembly and documentation. The script verifies exact SHA/workflow/status/source-artifact identity, compares metadata/legal files, reruns validators and original-data scans, then transforms ten source artifacts into seventeen release ZIPs: the direct PSP install ZIP, seven direct unified console install ZIPs, and nine remaining source-artifact wrappers. It writes exhaustive checksums/inventory in ignored atomic output and never tags, publishes, commits, or pushes |
| `docs/PLATFORM_MATRIX.md` | RetroArch `34c069f44f419b708c5362f96c54f959bc182dbe`; libretro-super `bc2b5463fe7e45a36295db54f74bc56b950dec4b`; libretro docs `7a0833c074b31d5438dce6ef6a94eea7f5805ec6`; live libretro buildbot indexes reviewed 2026-08-27 | New evidence ledger. Platform families, artifact forms, and expansion order were summarized from pinned official source/build recipes and live first-party buildbot directories. No source code or platform artwork was copied |

The implementation is intentionally split into modules so each derivation can be
traced rather than obscured in a monolithic port.

## Original copyrighted data

No `RESOURCE.MAP`, `RESOURCE.001`, `SCRANTIC.SCR`, Scene Explorer preview image,
original artwork, animation, music, or `sound<ID>.wav` sample is committed. The sole
bundled audio asset is the separately licensed CC0 ocean ambience identified above.
Local Web staging copies user-owned files into ignored `build/` output only; those
files must never enter a commit, release archive, or CI artifact and must be removed
before the server is shared. Tests construct synthetic fixtures or require the user to
pass a path to their own original data. The extracted route table stores behavioral
routing choices, not executable code or media bytes; its upstream extraction and GPL
lineage are nevertheless recorded above.

## Rules for future imports

1. Record repository URL, immutable revision, license, exact source paths, and author.
2. Preserve upstream copyright/license blocks on copied files.
3. Put `SPDX-License-Identifier: GPL-3.0-or-later` on new project source files.
4. State whether material was copied, translated, rewritten from behavior, or merely
   reviewed.
5. Never commit original Sierra/Dynamix content.
